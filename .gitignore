# Rust / Cargo
/target/
**/*.rs.bk
*.pdb

# Cargo.lock est **suivi** : le workspace produit un binaire (bin/server),
# les builds de prod doivent être reproductibles.

# Variables d'environnement / secrets (clés API, DSN Postgres…)
.env
.env.*
!.env.example

# Artefacts d'outillage
*.log

# OS
.DS_Store
Thumbs.db

# IDE (config personnelle, hors dépôt public)
/.idea/
/.vscode/
*.iml
