meta {
  name: Erreur — région inconnue (400)
  type: http
  seq: 14
}

get {
  url: {{baseUrl}}/v1/intensity/now?region=atlantide
  body: none
  auth: none
}

params:query {
  region: atlantide
}

assert {
  res.status: eq 400
  res.body.error: eq bad_request
}
