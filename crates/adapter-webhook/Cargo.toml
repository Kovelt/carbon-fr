[package]
name = "carbonfr-adapter-webhook"
version.workspace = true
description = "Adapter sortant carbon-fr : livraison de webhooks signés (Notifier), avec re-validation anti-SSRF (ADR-0016)."
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
carbonfr-core = { workspace = true }
async-trait = { workspace = true }
reqwest = { workspace = true }
tokio = { workspace = true, features = ["net", "time"] }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt", "rt-multi-thread"] }
# Serveur HTTP local pour les tests hermétiques de HttpNotifier (jamais dans le
# binaire de prod : dev-dependency uniquement).
axum = { workspace = true }
